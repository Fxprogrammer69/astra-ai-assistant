# Core (web mode — no Electron)
websockets>=12.0
anthropic>=0.20.0
numpy>=1.24.0
Pillow>=10.0.0

# Voice STT (optional but recommended)
openai-whisper>=20231117
SpeechRecognition>=3.10.0

# Optional CV / mic (disabled by default in fast mode)
# mediapipe>=0.10.0
# opencv-python>=4.8.0
# sounddevice>=0.4.6
